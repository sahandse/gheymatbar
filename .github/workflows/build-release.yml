name: Build gheymatbar Installable ZIP

on:
  push:
    branches: [main]
    paths:
      - '**/*.php'
      - 'assets/**'
      - 'includes/**'
      - 'languages/**'
      - 'readme.txt'
      - 'README.md'
      - '.github/workflows/build-release.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Read version
        id: version
        shell: bash
        run: |
          VERSION=$(grep 'Version:' gheymatbar.php | head -1 | sed 's/.*Version:[[:space:]]*//')
          test -n "$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: PHP syntax check
        shell: bash
        run: |
          set -e
          find . -type f -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l
      - name: Build ZIP
        shell: bash
        run: |
          rm -rf dist build
          mkdir -p dist build/gheymatbar
          rsync -a --exclude='.git' --exclude='.github' --exclude='build' --exclude='dist' ./ build/gheymatbar/
          cd build
          zip -qr ../dist/gheymatbar.zip gheymatbar
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gheymatbar-v${{ steps.version.outputs.version }}
          path: dist/gheymatbar.zip
      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          TAG="gheymatbar-v${VERSION}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" dist/gheymatbar.zip --clobber
          else
            gh release create "$TAG" dist/gheymatbar.zip --title "gheymatbar v${VERSION}" --notes "Installable WordPress ZIP for gheymatbar v${VERSION}."
          fi
